# NVIDIA GeForce RTX 3060 (12 GB)

Primary lab device. Every benchmark in this repository references this record.

## Identity

Queried from `cudaGetDeviceProperties` on 2026-08-15, not from a datasheet.

- **GPU:** NVIDIA GeForce RTX 3060, 12 GB
- **Architecture / compute capability:** Ampere GA106, **8.6** → `nvcc -arch=sm_86`
- **VRAM:** 12.00 GB
- **Date recorded:** 2026-08-15

### Device properties

| Property | Value | Why it matters |
| --- | --- | --- |
| SMs | 28 | A grid with fewer than 28 blocks leaves SMs idle regardless of block size |
| Warp size | 32 | The scheduling quantum; every coalescing question is asked per warp |
| Max threads / block | 1024 | Upper bound on a single block |
| Max threads / SM | 1536 (48 warps) | 1024-thread blocks waste a third of the SM — only one fits |
| Registers / SM | 65536 | **65536 / 1536 = 42.6 registers per thread** at full occupancy |
| Registers / block | 65536 | |
| Shared memory / SM | 102400 B (100 KB) | |
| Shared memory / block | 49152 B (48 KB) default | Opt-in carveout raises this on sm_86 |
| L2 cache | 2359296 B (2.25 MB) | Small relative to the tensors this lab will move |
| Memory bus | 192 bits @ 7501 MHz | |

**Theoretical peak bandwidth: 360.0 GB/s** — computed as `2 × 7501 MHz × 192 bits / 8`. This is the datasheet ceiling, **not** an achievable figure, and must never be used as the denominator in a "percentage of peak" claim. The measured figure below replaces it.

## Software

- **Driver:** 591.86 (Windows host; WSL uses the host driver through a 175 KB `libcuda.so` shim in `/usr/lib/wsl/lib/`)
- **CUDA toolkit:** 12.9.86 (`/usr/local/cuda-12.9`, installed inside WSL)
- **PyTorch:** 2.13.0+cu129
- **Triton:** 3.7.1
- **cuDNN:** not yet installed (was 9.20.0.48 before the 2026-08-23 reinstall below; PyTorch's bundled cuDNN is usable in the meantime, but the standalone install has not been redone)
- **NumPy:** 2.x (installed 2026-08-23, exact version not pinned yet)
- **Python:** 3.12.3 (venv at `/opt/venv-gpu`, root-owned — no personal Linux user account created yet)
- **OS:** Ubuntu 24.04.3 LTS on WSL2, kernel 6.6.87.2-microsoft-standard-WSL2
- **Host compiler:** gcc 13.3.0

Note: there is no MSVC on the Windows side, so the Windows `nvcc` 13.2 cannot compile. All CUDA work happens in WSL.

**2026-08-23 — WSL reinstalled from scratch.** The WSL launcher package (`MicrosoftCorporationII.WindowsSubsystemForLinux`) had a corrupted per-user COM registration (`REGDB_E_CLASSNOTREG`), unrelated to the GPU. Fixed by removing and reinstalling the package plus the Ubuntu-24.04 distro. This wiped the previous Linux instance entirely — the environment above (venv, CUDA toolkit, PyTorch) was rebuilt from nothing the same day, and everything so far has been done as `root` rather than a personal user account. Creating a real non-root user (`wsl -d Ubuntu-24.04` interactively, first run) is still outstanding.

## Settings

- **Clocks locked:** No — not yet attempted. Consumer card, expect boost variation under sustained load.
- **TF32 enabled:** `torch.backends.cuda.matmul.allow_tf32 = False` (PyTorch default); `torch.backends.cudnn.allow_tf32 = True`. Verified 2026-08-15. Any matmul benchmark must state which of these was in effect.
- **Persistence mode:** N/A under WSL.
- **Other processes on the GPU:** The Windows desktop compositor shares this GPU. It is not a headless device, and that is a source of run-to-run variance to account for.

## Measured peaks

Filling the rest of these in is a Phase 2 deliverable and blocks every roofline claim in this repository.

- Achievable memory bandwidth (measured): **305.21 GB/s** — STREAM Triad (`c = a + scalar*b`), 512 MB arrays (well beyond the 2.25 MB L2), PyTorch + `torch.cuda.Event` timing, 50 repeats, 2026-08-23. 85% of the 360.0 GB/s datasheet ceiling. Method: [learning/phase-01-computer-architecture/examples/06-gpu-bandwidth-latency/bandwidth_gpu.py](../../learning/phase-01-computer-architecture/examples/06-gpu-bandwidth-latency/bandwidth_gpu.py)
- Global memory latency (measured): **289.7 ns** at the 262144 KB (256 MB) buffer — single-thread (`<<<1,1>>>`) Sattolo pointer chasing, `cudaEvent` timing, 2026-08-23. Plateau reached by the 8192 KB buffer (~289 ns); smaller buffers show lower latency (40.7 ns at 16 KB, 103.7 ns at 128 KB, 272.5 ns at 2048 KB), consistent with cache effects at smaller sizes. Method: [learning/phase-01-computer-architecture/examples/06-gpu-bandwidth-latency/latency_gpu.cu](../../learning/phase-01-computer-architecture/examples/06-gpu-bandwidth-latency/latency_gpu.cu)
- FP32 throughput (measured): **not measured**
- FP16 / BF16 Tensor Core throughput (measured): **not measured**
- Kernel launch overhead (measured): **not measured** — method ready at [learning/phase-02-gpu-architecture/examples/03-launch-overhead](../../learning/phase-02-gpu-architecture/examples/03-launch-overhead/); fill only after executing it on this machine

### Little's Law, applied to the measured numbers above

Using 32 bytes/request (Ampere L2 sector size, not the 128-byte full line — a sparse single-thread access pattern triggers one sector fetch, not the whole line):

```text
Throughput desejado = 305.21 GB/s / 32 bytes = ~9.54 billion requests/s
Concorrência necessária = 9.54e9 × 289.7ns = ~2,764 requests in flight
```

For context, this device can have at most 28 SMs × 1536 threads/SM = 43,008 threads resident — so saturating measured bandwidth needs roughly 6% of max theoretical occupancy's worth of concurrent requests, assuming each thread keeps one request in flight. Script: [learning/phase-01-computer-architecture/examples/06-gpu-bandwidth-latency/little_law_gpu.py](../../learning/phase-01-computer-architecture/examples/06-gpu-bandwidth-latency/little_law_gpu.py)

### Phase 2 architecture suite

The architecture experiments live in
[`learning/phase-02-gpu-architecture/`](../../learning/phase-02-gpu-architecture/). They
cover device inventory, launch overhead, coalescing, divergence, register pressure,
barriers and atomic contention. Results remain deliberately blank until each executable is
run on this device; compiling a benchmark is not a measurement.

## Notes

### Numerical baseline: CPU vs GPU fp32 disagree, and that is correct

Measured 2026-08-15 with a 512×512 fp32 matmul (`torch.manual_seed(0)`), TF32 off:

| Quantity | Value |
| --- | --- |
| Median \|result\| | 15.24 |
| Max absolute error | 4.77e-05 |
| Max relative error (where \|ref\| > 1) | 2.09e-05 |
| Elements failing `allclose(rtol=1e-4, atol=1e-5)` | 11 of 262144 (0.004%) |
| Median \|ref\| **at the failing elements** | 0.0244 |

The failures are concentrated entirely on near-zero outputs. `allclose` tests `|a-b| <= atol + rtol*|b|`; where `|b| ≈ 0` the `rtol` term vanishes and `atol` alone decides. Raising `atol` to 1e-4 passes.

The disagreement itself is not a bug and not TF32 — it is floating-point addition being non-associative, with the GPU accumulating in a different order than the CPU.

**Consequence for this lab:** a correctness test that reports pass/fail from a single tolerance is not enough. Report max absolute error, max relative error, and the magnitude of the reference at the worst element. A kernel that fails only where the reference is ~0.02 is a different situation from one that fails where the reference is ~15.

### Environment gotchas

- Privileged commands must run as `wsl -d Ubuntu -u root`; `sudo` blocks forever when there is no tty.
- Never install an NVIDIA driver inside WSL — it breaks the host passthrough.
- CUDA env lives in `/etc/profile.d/cuda.sh`, not `~/.bashrc`, because Ubuntu's `.bashrc` returns early for non-interactive shells.
- WSL is capped at ~7.7 GB RAM (50% of host). Raise via `C:\Users\mathe\.wslconfig` if it becomes a limit.
