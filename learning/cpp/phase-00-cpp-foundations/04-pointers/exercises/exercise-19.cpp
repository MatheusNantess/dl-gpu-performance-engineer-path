/*
Exercise 19 — Predict: the whole model
Difficulty: HARD

Goal:
Cumulative prediction test covering the module.

Requirements:
- Construct a program that combines: an array, several pointers into it,
  pointer arithmetic, a function taking a pointer, a const pointer, and one
  deliberate out-of-bounds read.
- BEFORE compiling, write out the complete expected output.
- Then run it and account for every difference.
- Then run under -fsanitize=address and see what it catches that you did not.

Questions before coding:
1. Which line is undefined behaviour, and did your prediction assume a value for it?
2. Did the sanitizer catch everything you knew was wrong?
3. Which prediction were you least sure about, and why?

Why this matters for CUDA:
Reading unfamiliar kernel code and knowing what it does before running it is the working skill. This is that, scaled down.
*/

#include <iostream>

int main() {
    // TODO: implement
    return 0;
}