/*
Exercise 06 — Traverse by pointer
Difficulty: EASY

Goal:
Write the same loop two ways.

Requirements:
- Given an int array, print all elements using index notation.
- Print them again using a moving pointer and no index variable at all.
- Sum them both ways.
- Print the pointer value at each step of the pointer version.

Questions before coding:
1. What is the loop condition in the pointer version?
2. Is forming the address one past the end legal? Is dereferencing it?
3. Do the two versions generate different machine code? How could you check?

Why this matters for CUDA:
CUDA code uses both forms constantly, often within the same kernel.
*/

#include <iostream>

int main() {
    // TODO: implement
    return 0;
}