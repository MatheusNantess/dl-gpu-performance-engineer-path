/*
Exercise 10 — Same algorithm, both forms
Difficulty: MEDIUM

Goal:
Fluency in converting between index and pointer style.

Requirements:
- Implement each of these TWICE, once with indices and once with pointer
  arithmetic only (no [] at all):
    sum, max, count occurrences of a value, reverse in place, copy.
- Verify both versions agree on the same input.
- Do not use STL algorithms.

Questions before coding:
1. Which version reads more naturally to you? Does that change by the fifth one?
2. In the reverse function, what does the pointer version look like?
3. Is there any performance difference? How would you find out?

Why this matters for CUDA:
Converting between the two forms without thinking is the working definition of pointer fluency.
*/

#include <iostream>

int main() {
    // TODO: implement
    return 0;
}