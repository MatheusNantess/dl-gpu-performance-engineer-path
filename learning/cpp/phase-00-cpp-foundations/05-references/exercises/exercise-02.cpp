/*
Exercise 02 — Reassignment trap
Difficulty: EASY

Goal:
Find out what r = y actually does.

Requirements:
- Declare two ints x and y with different values, and a reference r bound to x.
- Predict, in writing, what  = y; does BEFORE running.
- Then run it and print x, y, and the addresses of x, y and r.
- Do the equivalent with a pointer and compare.

Questions before coding:
1. After r = y, does r refer to y?
2. What value does x hold, and why?
3. What would the pointer version have done differently?

Why this matters for CUDA:
The pointer/reference distinction matters when reading host-side wrapper code around kernels.
*/

#include <iostream>

int main() {
    // TODO: implement
    return 0;
}