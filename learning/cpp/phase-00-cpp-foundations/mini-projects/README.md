# Mini Projects

Two projects, after the challenges. Each is larger than an exercise and has a specification rather than a single task.

| Project | Purpose |
| --- | --- |
| [cpu-array-kernel-lab](cpu-array-kernel-lab/) | Write, on the CPU, the six operations that become the first CUDA kernels |
| [memory-explorer](memory-explorer/) | Investigate memory empirically and record what you find |

Do `cpu-array-kernel-lab` first. It is the direct rehearsal for Phase 3.