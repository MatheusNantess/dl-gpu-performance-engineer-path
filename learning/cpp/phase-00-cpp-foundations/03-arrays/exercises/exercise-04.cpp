/*
Exercise 04 — Reverse in place
Difficulty: EASY

Goal:
Index arithmetic from both ends.

Requirements:
- Reverse an int array in place, without a second array.
- Handle both even and odd lengths correctly.
- Print before and after.
- Do not use std::reverse or std::swap.

Questions before coding:
1. How many swaps for n elements? Write the formula.
2. What happens if the loop runs to n instead of n/2?
3. What is the middle element in an odd-length array, and does it need swapping?

Why this matters for CUDA:
Index arithmetic from both ends appears in reduction kernels, where threads pair up across a range.
*/

#include <iostream>

int main() {
    // TODO: implement
    return 0;
}