/*
Exercise 01 — Basic function set
Difficulty: EASY

Goal:
Get comfortable with signatures, return types and prototypes.

Requirements:
- Write: square(int), cube(int), max_of_two(int,int), is_even(int).
- Declare all four as prototypes ABOVE main, define them BELOW main.
- Call each from main and print the results.
- Do not use any STL algorithm.

Questions before coding:
1. Why does the prototype have to appear before main?
2. What happens if a prototype and its definition disagree on a parameter type?
   Try it deliberately and record the error.
3. Is is_even returning int or bool a meaningful difference here?

Why this matters for CUDA:
Kernels are declared in headers and defined in .cu files. The split is the same.
*/

#include <iostream>

// TODO: prototypes here

int main() {
    // TODO: implement
    return 0;
}

// TODO: definitions here